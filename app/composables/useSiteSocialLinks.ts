export interface SiteSocialLink {
  label: string
  to: string
  icon?: string
}

// Placeholder until socials are defined in app config / copy docs.
export const useSiteSocialLinks = () => [] as const
